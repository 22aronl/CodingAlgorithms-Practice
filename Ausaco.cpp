/*
ID: chiragz1
LANG: C++
PROB: ${FILE}
*/
#include <bits/stdc++.h>
#define ll long long
#define ull unsigned long long
#define ui unsigned int
#define f first
#define s second
#define mp make_pair
#define pb push_back
#define endl "\n"
#define INF 10000000
using namespace std;
typedef pair<int, int> pii;
typedef pair<ll, ll> pll;


int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	#ifndef local
	freopen((string("${FILE}").substr(0, string("${FILE}").size() - 4) + ".in").c_str(), "r", stdin);
	freopen((string("${FILE}").substr(0, string("${FILE}").size() - 4) + ".out").c_str(), "w", stdout);
	#endif
	
}